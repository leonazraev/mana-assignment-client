import Vue from 'vue'
import Vuex from 'vuex'
Vue.use(Vuex)
const api = Vue.prototype.$api;

const store = new Vuex.Store({
    state: {
     
    },
    mutations: {
    },
    actions: {
     
    },
    getters: {
    }
  })

export default store;